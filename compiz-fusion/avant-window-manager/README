Avant Window manager

Requires pyxdg, this build is set to window manager agnostic, read the script
to see where to change it to be xfce or gnome (if required).  Also disables
gconf support, again read the script to discren how to re-enable that.
